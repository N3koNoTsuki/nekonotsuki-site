// Canonical production URL — the repo's homepage field on GitHub (Vercel
// deployment). Override with NEXT_PUBLIC_SITE_URL if the domain ever changes.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://nekonotsuki-site.vercel.app";
